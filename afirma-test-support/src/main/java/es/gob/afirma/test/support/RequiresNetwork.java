package es.gob.afirma.test.support;

/** Categor&iacute;a JUnit para pruebas que necesitan acceso a red (servicios externos reales). */
public interface RequiresNetwork {
	// Interfaz marcadora sin metodos
}
