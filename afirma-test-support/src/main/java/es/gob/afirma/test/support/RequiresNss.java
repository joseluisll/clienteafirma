package es.gob.afirma.test.support;

/** Categor&iacute;a JUnit para pruebas que necesitan un almac&eacute;n NSS de
 * Mozilla Firefox (o NSS compartido del sistema) instalado y accesible. */
public interface RequiresNss {
	// Interfaz marcadora sin metodos
}
